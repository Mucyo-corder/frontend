# Used by `vite build` — browser calls this host for /api/... (no trailing slash).
# Backend URL + deployed SPA (for your notes). CORS is configured on the API.
VITE_API_BASE_URL=https://sober-watch.onrender.com
